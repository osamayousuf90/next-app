import { NextResponse } from "next/server";


export default function middleware(req) {
  //  let verify = window.localStorage?.getItem("isLogged");
  //   let url = req.url;


  //   if (!verify && url.includes("/")) { 
  //     return NextResponse.redirect("http://localhost:3000/")
  //   }
}